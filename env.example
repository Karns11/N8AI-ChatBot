# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (SQLite for Django)
# The warehouse database is configured separately in settings.py

# PostgreSQL Warehouse Database Settings
DB_NAME=warehouse
DB_USER=postgres
DB_PASSWORD=your-password-here
DB_HOST=localhost
DB_PORT=5432

# OpenAI Settings
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4

# Optional: Use different model for testing
# OPENAI_MODEL=gpt-3.5-turbo 